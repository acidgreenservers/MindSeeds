import { SeedData } from '@/types';
export const data: SeedData = {
  id: 'interface-reality',
  text: '"The interface is the only reality"',
  category: 'arch',
  pattern: 'Encapsulation — components must not know how their neighbors work — only what they promise.',
  deployWhen: 'Defining APIs, microservices, or team responsibility boundaries.'
};
export default () => null;
