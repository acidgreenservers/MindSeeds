import { SeedData } from '@/types';
export const data: SeedData = {
  id: 'delete-key',
  text: '"Build for the delete key"',
  category: 'arch',
  pattern: 'Decomposability — a module is only good if it can be removed without the whole system collapsing. Code is a liability.',
  deployWhen: 'System design, feature development, dependency management, any large-scale project.'
};
export default () => null;
