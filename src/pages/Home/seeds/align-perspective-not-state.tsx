import { SeedData } from '@/types';

export const data: SeedData = {
  id: 'arch-align-perspective-not-state',
  text: "Align perspective, not state, to prevent feedback loop collapse",
  category: 'arch',
  family: 'Structure',
  pattern: 'Perspective Alignment: Ensures system stability by adjusting relationships rather than immutable state.',
  deployWhen: 'when immutable state blocks progress, when recursive logic creates drift, when user interactions stall, when configuration cannot be directly edited'
};
