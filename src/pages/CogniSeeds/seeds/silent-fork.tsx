import { SeedData } from '@/types';
export const data: SeedData = {
  id: 'silent-fork',
  text: '"Assumption is a silent fork"',
  category: 'cogni',
  pattern: 'Hidden State Detection — every unverified assumption creates a divergent path invisible to both parties.',
  deployWhen: 'Collaborative reasoning, multi-agent systems, anywhere two models of reality must stay synced.'
};
export default () => null;
