import { SeedData } from '@/types';
export const data: SeedData = {
  id: 'invariants',
  text: '"A reasoning model listens for invariants"',
  category: 'cogni',
  pattern: 'Signal Selection — filter noise by anchoring to what cannot change, not what seems to change.',
  deployWhen: 'Prompt design, system audits, any domain where signal-to-noise ratio is low.'
};
export default () => null;
