import { SeedData } from '@/types';
export const data: SeedData = {
  id: 'map-both-sides',
  text: '"Map both sides before crossing"',
  category: 'cogni',
  pattern: 'Alignment Verification — ensure internal model matches external reality before execution.',
  deployWhen: 'API integration, debugging, argument construction, any cross-system handoff.'
};
export default () => null;
